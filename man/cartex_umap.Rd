% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/visualize.R
\name{cartex_umap}
\alias{cartex_umap}
\title{Side-by-side UMAP: cell annotation and CARTEx score}
\usage{
cartex_umap(
  x,
  input = NULL,
  annotation = NULL,
  score = "cartex_score",
  compute = TRUE,
  cd8_only = TRUE,
  renormalize = FALSE,
  na_colour = "grey85",
  point_size = 0.4,
  file = NULL
)
}
\arguments{
\item{x}{A result from \code{\link[=cartex_sc]{cartex_sc()}}: a scored Seurat object, or the scored
data.frame (single-cell level).}

\item{input}{Optional original input path (e.g. the \code{.h5ad}); used to fetch an
existing UMAP when \code{x} is a data.frame without UMAP columns.}

\item{annotation}{Metadata column to colour the left panel by. Defaults to the
CD8 annotation column from the report, else a detected cell-type column.}

\item{score}{Score column for the right panel (default \code{"cartex_score"}).}

\item{compute}{If \code{TRUE} (default), compute a UMAP for a Seurat object that
lacks one. Has no effect for data.frame input (the full matrix is not loaded).}

\item{cd8_only}{If \code{TRUE} (default), the CARTEx score is shown on the right
panel only for CD8+ T cells; every other cell type (CD8-negative, ambiguous,
non-T, unknown) is greyed, because the CARTEx score is only biologically
valid for CD8+ T cells and colouring other cells by it is misleading. All
cells are still drawn on both panels, so the population context is preserved.
Set \code{cd8_only = FALSE} to colour every cell by its score. Requires a
\code{cd8_status} column (present in any \code{cartex_sc()} result).}

\item{renormalize}{If \code{TRUE} and \code{cd8_only = TRUE}, re-Z-score the raw
per-cell score \emph{within the CD8+ population} before plotting, so the colour
scale reflects variation among the cells CARTEx is valid for rather than the
whole-object distribution (which is diluted by unscored non-CD8 cells).
Default \code{FALSE} (show the score column as computed, just masked).}

\item{na_colour}{Colour for cells that are not scored on the right panel
(default "grey85").}

\item{point_size}{Point size (default 0.4).}

\item{file}{Optional path to save the figure (png/pdf via \code{ggplot2::ggsave}).}
}
\value{
A patchwork/ggplot object (invisibly if \code{file} is given).
}
\description{
Produces a two-panel UMAP from a \code{\link[=cartex_sc]{cartex_sc()}} result -- the left panel
coloured by cell-type annotation, the right by CARTEx score -- on identical
coordinates. If the object already contains a UMAP embedding (Seurat
reduction, AnnData \code{obsm/X_umap}, or \code{UMAP_1}/\code{UMAP_2} columns) it is reused;
otherwise, for a Seurat object, a UMAP is computed with default Seurat
parameters.
}
