% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cartex_sc.R
\name{cartex_sc}
\alias{cartex_sc}
\title{Compute the CARTEx exhaustion score for single-cell data}
\usage{
cartex_sc(
  input,
  level = c("single_cell", "pseudobulk"),
  cd8 = c("flag", "restrict"),
  cd8_method = c("auto", "metadata", "markers", "none"),
  cd8_column = NULL,
  group_by = NULL,
  pseudo_replicates = 1L,
  seed = 1L,
  normalization = c("auto", "lognorm", "assume_normalized"),
  assay = NULL,
  layer = NULL,
  min_match_frac = 0.5,
  return = c("object", "scores", "report"),
  verbose = TRUE
)
}
\arguments{
\item{input}{A file path (\code{.rds}, \code{.RData}, \code{.h5ad}, \code{.h5Seurat}, \code{.loom}),
an in-memory Seurat object, or a gene x cell matrix.}

\item{level}{\code{"single_cell"} (default) or \code{"pseudobulk"}.}

\item{cd8}{How to use the CD8+ classification: \code{"flag"} (default) scores all
cells and records a \code{cd8_status} column; \code{"restrict"} scores only confident
CD8+ T cells.}

\item{cd8_method}{\code{"auto"} (default: cell-type annotation if present, else
marker genes), \code{"metadata"}, \code{"markers"}, or \code{"none"}.}

\item{cd8_column}{Optional explicit cell-type annotation column name.}

\item{group_by}{For pseudobulk: metadata column(s) to aggregate by. If \code{NULL},
a cluster-like column is auto-detected.}

\item{pseudo_replicates}{For pseudobulk: number of random replicate bins per
group (default 1 = none).}

\item{seed}{Random seed for pseudo-replicate assignment (default 1).}

\item{normalization}{\code{"auto"} (detect counts vs normalized), \code{"lognorm"}, or
\code{"assume_normalized"}.}

\item{assay, layer}{Optional assay / layer (Seurat / h5Seurat).}

\item{min_match_frac}{Warn if fewer than this fraction of CARTEx genes match.}

\item{return}{\code{"object"} (default), \code{"scores"}, or \code{"report"}.}

\item{verbose}{Print the report summary when done (default TRUE).}
}
\value{
Depending on \code{return} and \code{level}: the annotated object, a scores
data.frame, or the \code{cartex_report}.
}
\description{
Scores single-cell data with the published CARTEx CD8+ T-cell exhaustion
signature (200 PCA-weighted genes). Built to run on a laptop even for large
files: for HDF5-backed formats it reads only the CARTEx genes (plus a few CD8
marker genes) from disk rather than loading the whole matrix.
}
\details{
Because CARTEx is only valid for CD8+ T cells, every cell is classified as
CD8+ or not before scoring (see \code{cd8}/\code{cd8_method}). And because a per-cell
(or per-cluster) score inherits choices baked into the input object, every
decision the function makes is captured in a \code{cartex_report} object so the
assumptions behind the score are explicit and auditable.
}
\section{Analysis level}{

With \code{level = "single_cell"} (default) each cell is scored individually. With
\code{level = "pseudobulk"}, cells are grouped (by \code{group_by}, e.g. cluster), their
raw counts are summed and LogNormalized, and the resulting pseudobulk profiles
are scored -- following the reference CARTEx pipeline
(Seurat::AggregateExpression-style aggregation). Set \code{pseudo_replicates > 1}
to randomly split each group into replicate pseudobulk samples.
}

\section{Score definition}{

For each cell or pseudobulk sample, the raw score is the dot product of its
log-normalized expression with the 200 CARTEx weights (\code{cartex_score_raw}).
\code{cartex_score} is that value Z-scored across all scored units, and
\code{cartex_score_int} is the Z-score rounded and capped at +/-4 (matching the
reference \code{integerize}). The Z-score is population-relative, so scores are
comparable only within one scored object.
}

\seealso{
\code{\link[=cartex_score]{cartex_score()}} for bulk data, \code{\link[=read_single_cell]{read_single_cell()}} for loading only.
}
